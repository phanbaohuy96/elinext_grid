name: elinext_grid
description: "Elinext Flutter test task — paginated GridView of picsum images."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  flutter_bloc: ^9.1.1
  bloc_concurrency: ^0.3.0
  equatable: ^2.0.7
  freezed_annotation: ^3.1.0
  cached_network_image: ^3.4.1
  get_it: ^8.2.0
  injectable: ^2.6.0
  intl: ^0.20.2
  go_router: ^14.6.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  build_runner: ^2.7.1
  freezed: ^3.2.3
  injectable_generator: ^2.9.1
  bloc_test: ^10.0.0

flutter:
  uses-material-design: true
  generate: true
